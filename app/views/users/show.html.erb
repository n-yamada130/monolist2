<% content_for :head do %>
  <title><%= @user.name %> - Monolist</title>
  <meta property="description" content="<%= @user.name %>のモノページ。<%= @user.name %>の欲しいモノ、持っているモノを表示しています。" />
<% end %>
<%= render 'profile' , user: @user %>
<%= render 'items/list' , items: @items %>